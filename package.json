{
  "name": "movie-to-text",
  "version": "1.0.0",
  "description": "YouTube動画のテキストを簡単に抽出するツール",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --port 8788",
    "deploy:pages": "wrangler pages deploy public",
    "deploy:worker": "wrangler deploy",
    "dev:worker": "wrangler dev"
  },
  "dependencies": {
    "youtube-transcript": "^1.3.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
